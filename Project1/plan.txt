1. separate class for every figure
2. override toString to put to text file 
3. save every object as a line, with first word as identifier of shape 
4. write fromString for every class to get other information from line
5. setTimer
6. find centerfisrt and then determine other points
7. use proportions to adjust to changes of window
8. create color generator for every class, not in each one 

questions: 
    * how many figures?
    * too separate .class files?
    * can they intersect with each other?
    * how adjust to chages of frame?
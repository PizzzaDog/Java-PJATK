public class NumberFun {
    static void print(Object obj) {
        System.out.println(obj);
    }
     public static void main (String[] args) {
      print (Math.sqrt(120)%1);
   /* long sq= 120;
    double n = Math.sqrt(sq);
    double next;
    double a;
        if (n == (int)n){
            a = (Math.sqrt(sq)+1);
            next = (Math.pow(a,2.0));
        }else {
            next = -1;
        }
        System.out.println(next);*/

         }
     }
